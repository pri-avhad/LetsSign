name: abc
description: A 3d implementation in flutter.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  carousel_pro: ^1.0.0
  cupertino_icons: ^0.1.3
  camera: ^0.5.8+2
  path: ^1.6.4
  #path_provider: ^1.6.11
  http: ^0.12.2
  agora_rtc_engine: ^3.3.2
  permission_handler: ^6.1.1
  agora_rtm: ^0.9.14
  path_provider: ^2.0.1
  native_screenshot: ^1.0.0

  image_sequence_animator:
    git:
      url: 'https://github.com/papmodern/flutter-image-sequence-animator.git'
      path: 'image_sequence_animator'

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets:
    - assets/images/
